import { Button } from "@/components/ui/button";

interface RaceResultDialogButtonProps {}

const RaceResultDialogButton: React.FC<RaceResultDialogButtonProps> = () => {
  return <Button type="submit">Zapisz</Button>;
};

export default RaceResultDialogButton;
